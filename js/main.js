/* ===========================================================
   진입점
   - DOM 로드가 끝나면 타이틀 화면을 띄웁니다.
   =========================================================== */

window.addEventListener('DOMContentLoaded', () => {
  window.TitleScene.render();
});
